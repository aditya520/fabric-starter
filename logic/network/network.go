package network

import (
	"fabric_starter/models"
)

func CreateNetwork(networkObj *models.Object) (string, error) {

	//fileName := "fixtures/" + networkObj.Name + ".json"

	//bytes, err := json.MarshalIndent(networkObj, "", "    ")

	return "Success", nil
}
